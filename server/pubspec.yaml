name: itinera_server
description: Backend di sincronizzazione per Itinera (login + document store).
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  crypto: ^3.0.3

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
